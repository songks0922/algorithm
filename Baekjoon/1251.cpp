#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  
  string word, a, b, c, answer = "";

  cin >> word;

  // for (int i = 0; i < word.length)
  
  return 0;
}